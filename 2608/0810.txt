
| パターンID | リソース分類 | リソース | 面区分 | 改修パターン | 変更内容の詳細 | 改修時の考慮事項 | 無停止リリース可否 | 理由・前提 | ステータス |
|---|---|---|---|---|---|---|---|---|---|
| R-07 | Lambda | 既存共通／新規共通Lambda関数<br>対象：Func_cmpf_send_delivery_logs_dev／Func_cmpf_firehose_put_dev 等 | 既存共通 | 改修（日常） | Lambda関数コードの更新（後方互換性を保った変更） | ①本Lambdaは単一実体でA/B両面から共用されるため、デプロイ直後から両面の呼び出しが新コードで処理される<br>②新旧バージョンアプリ共存期間中、旧バージョン側からの呼び出し（イベント形式・パラメータ）に対しても新コードが正しく処理できる実装であることが前提 | 可 | ①本変更は新旧双方の呼び出し形式・期待する処理結果に対して後方互換性を保っており、旧バージョン側からの呼び出しであっても新コードで正常に処理されるため | 確定 |
| R-08 | Lambda | 既存共通／新規共通Lambda関数（R-07と同一対象） | 既存共通 | 改修（日常） | Lambda関数コードの更新（後方互換性を持たない変更、例：レスポンス形式変更、呼び出し元が未対応の新規必須パラメータ追加等） | ①本Lambdaは単一実体のため、非活動面のみに変更を限定する手段が存在しない | 不可 | ①デプロイ直後から旧バージョン側の呼び出しも新コードで処理されるが、新コードが旧バージョン側の呼び出し形式・期待結果に対応していないため、共存期間中の旧バージョン側処理が異常終了、またはデータ不整合を起こすため | 確定 |
| R-09 | Lambda | 既存共通／新規共通Lambda関数（R-07と同一対象） | 既存共通 | 改修（日常） | Lambda関数の環境変数変更（後方互換性を保った変更、例：追加のみでロジック分岐に影響しない変更） | ①環境変数変更も単一実体への即時反映であり、A/B両面からの呼び出しに同時に影響する<br>②warm containerが一時的に旧環境変数を保持する可能性はあるが、数分以内に入れ替わる | 可 | ①変更後の挙動が旧バージョン側からの呼び出しに対しても互換性を保っているため、共存期間中も両面の処理が正常に継続するため | 確定 |
| R-10 | Lambda | 既存共通／新規共通Lambda関数（R-07と同一対象） | 既存共通 | 改修（日常） | Lambda関数の環境変数変更（後方互換性を持たない変更、例：外部接続先の切替により旧バージョン側が想定する挙動と異なる結果を返す変更） | ①同上、単一実体のため面を限定した適用ができない | 不可 | ①変更後の挙動が旧バージョン側の呼び出しが期待する処理結果と異なるため、共存期間中の旧バージョン側処理に不整合が発生するため | 確定 |


















































