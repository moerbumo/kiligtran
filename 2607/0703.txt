以下、最終版です。番号は B面インフラ確認Case（10-18相当）の後に続く前提で No.19, No.20 としています（実際の数字はB面Case数確定後に調整してください）。

No.	チェック項目	確認箇所	操作	確認要領・条件	想定結果	エビデンス	備考
19	A面：CI/CDパイプライン全体確認（PR→デプロイ→切流未実施確認）	GitHub / CodePipeline / ECR / ECSコンソール / EC2コンソール（リスナー）	①.env（またはPR対象ブランチ）にてTARGET=Aを設定し、対象ブランチ（CUSCOST）へPRを作成・マージする。②CodePipelineが自動起動することを確認する。③build.shによって生成されたイメージタグ（命名規則：組件名+TARGET+バージョン、例：v1a系）がECRへpushされていることを確認する。④CDKデプロイ完了後、対象ECS Service（A面）が原地更新（新タスク定義でのローリングアップデート）されていることを確認する。⑤DeployApiGwListenerApprovalステップで一時停止することを確認し、表示されている変更内容（対象TG・切替ルール）が想定通りであることを確認する。⑥Approvalを却下（Reject）する。⑦却下後、Pipelineの実行が停止し、ALBリスナーのルール切替が発生していないことを確認する	・Pipelineがエラーなく各ステップを通過すること
・イメージタグが想定命名規則で生成されていること
・ECS ServiceがCREATE済みのTask定義で原地更新されること（Stack再作成ではないこと）
・Approvalステップで確実に一時停止すること
・却下操作によりPipelineが正常終了すること	・CodePipeline実行画面（各ステージSucceeded）
・ECRのイメージタグ一覧
・ECSサービスのデプロイ履歴（原地更新であることが分かる画面）
・Approval一時停止画面（対象情報表示）
・却下後のPipeline最終状態画面
・却下後のリスナールール画面（変更前と同一であることが分かる比較）	本Caseの実施前提として、Java側健康チェック対応分岐を一時的にマージ済みであること	
20	B面：CI/CDパイプライン全体確認（差分デプロイ確認含む、切流未実施確認）	GitHub / CodePipeline / ECR / ECSコンソール / EC2コンソール（リスナー）	①.env（またはPR対象ブランチ）にてTARGET=Bを設定し、併せて対象コンポーネントのうち1つのみバージョン番号を+1する。②対象ブランチ（CUSCOST）へPRを作成・マージする。③CodePipelineが自動起動することを確認する。④ECRを確認し、バージョンを上げた対象コンポーネントのみ新規イメージが生成されていること、その他のコンポーネントは新規イメージが生成されていないこと（差分デプロイが機能していること）を確認する。⑤CDKデプロイ完了後、対象ECS Service（B面）が原地更新されていることを確認する。⑥DeployApiGwListenerApprovalステップで一時停止することを確認し、表示されている変更内容が想定通りであることを確認する。⑦Approvalを却下（Reject）する。⑧却下後、Pipelineの実行が停止し、ALBリスナーのルール切替が発生していないことを確認する	・Pipelineがエラーなく各ステップを通過すること
・バージョンを上げた対象コンポーネントのみECRに新規イメージが生成され、他のコンポーネントには新規イメージが生成されないこと（差分デプロイロジックが両面化後も正しく機能していることの証明）
・ECS ServiceがCREATE済みのTask定義で原地更新されること
・Approvalステップで確実に一時停止すること
・却下操作によりPipelineが正常終了すること	・CodePipeline実行画面（各ステージSucceeded）
・ECRのイメージタグ一覧（対象コンポーネントのみ新規タグ、他は変更なしであることが分かる比較画面）
・ECSサービスのデプロイ履歴（原地更新であることが分かる画面）
・Approval一時停止画面（対象情報表示）
・却下後のPipeline最終状態画面
・却下後のリスナールール画面（変更前と同一であることが分かる比較）	本CaseはA面Case(No.19)と独立したPR・Pipeline実行であるため、事前にA面分のPipelineが完了（却下済み）していることを前提とする	
テスト実施後の後片付け（Case外・文章記載）

本テスト（Case19・20）実施後、以下の手順で環境を復旧する。

Approval却下により生成されたPipeline実行はキャンセル済みのため、追加のキャンセル操作は不要。
cdk destroyにより、本テストで新規作成したA面・B面インフラスタック（Case1-18相当）を削除する。
事前準備で一時的に変更したECSタスク起動数関連設定（4箇所）を元の値に復元する。
マージした Java側健康チェック対応分岐について、本テスト用に取り込んだ内容を元のブランチ状態に復元する（テスト用の一時マージであり、正式マージではないことを明記）。
復旧後、対象コンポーネント以外（他チーム管理分含む）への影響がないことを確認する。

この内容で最終版としてよろしいですか？何か調整したい点があれば教えてください。